[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra --strict-markers
filterwarnings =
    # FastAPI on_event deprecation (known, ноут в audit)
    ignore::DeprecationWarning:app.routes.core
